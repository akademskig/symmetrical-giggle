{
  "name": "kompare-zadatak",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:fix": "prettier -w .",
    "docker:up": "docker-compose -f docker-compose.yml up --build -d",
    "docker:down": "docker-compose -f docker-compose.yml down --remove-orphans",
    "seedDb": "docker exec -it kompare-zadatak-backend-1 sh -c 'npm run seedDb'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
